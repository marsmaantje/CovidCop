using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Influence : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // need see all npc close 
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
